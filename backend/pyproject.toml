[project]
name = "applyflow-ai-backend"
version = "1.0.0"
description = "ApplyFlow AI Backend - FastAPI"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
ignore = ["E501"]
